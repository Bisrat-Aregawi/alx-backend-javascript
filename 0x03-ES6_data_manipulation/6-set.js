/**
  * setFromArray - Function creates a set from passed array object
  * @param {Array} arr - Array with any element types
  * @returns {Set} A set from given array
  */
export default function setFromArray(arr) {
  return new Set(arr);
}
